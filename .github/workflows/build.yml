name: Build

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore src/GrevUltraVNC/GrevUltraVNC.csproj
      - name: Build
        run: dotnet build src/GrevUltraVNC/GrevUltraVNC.csproj --configuration Release --no-restore
