<Window x:Class="GrevUltraVNC.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GrevUltraVNC"
        Width="430" Height="190" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource AppWindowStyle}"
        Background="{DynamicResource WindowBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Border Background="{DynamicResource WindowBrush}" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="PromptTitleText" FontSize="18" FontWeight="Bold" />
            <TextBlock x:Name="PromptText" Grid.Row="1" Foreground="{DynamicResource MutedTextBrush}" Margin="0,5,0,12" TextWrapping="Wrap" />
            <TextBox x:Name="ValueBox" Grid.Row="2" VerticalAlignment="Top" KeyDown="ValueBox_KeyDown" />
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Style="{StaticResource SecondaryButton}" Content="Cancel" Click="Cancel_Click" Margin="0,0,8,0" />
                <Button Style="{StaticResource PrimaryButton}" Content="OK" Click="Ok_Click" MinWidth="86" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
